{
  "name": "@repo/simulator",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify src/index.ts --outfile sim-exe",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13"
  }
}
