info:
  name: create-service
  type: http
  seq: 2

http:
  method: post
  url: "{{BASE_URL}}/api/services"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Test Service"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
